package com.flavormetrics.api.entity.user.impl;

import com.flavormetrics.api.entity.user.User;

public class Admin extends User {

    protected <T extends Builder<T>> Admin(Builder<T> builder) {
        super(builder);
    }
}